
<%= simple_form_for(@list) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :product %>
    <%= f.input :quantity %>
    <%= f.input :bought %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
